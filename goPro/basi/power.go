package main

import (
	"fmt"
	"math"
)

func main() {
	for i := 0; i < 5; i++ {
		// fmt.Println(math.Pow(float64(i), 2))

		// fmt.Println("hello\n")
		// fmt.Println(math.Abs(float64(i)))
		

	}

}
